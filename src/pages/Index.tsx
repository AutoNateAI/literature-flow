import Dashboard from "./Dashboard";

const Index = () => {
  // In a real app, check authentication here and redirect to login if needed
  // For now, we'll show the dashboard directly
  return <Dashboard />;
};

export default Index;
